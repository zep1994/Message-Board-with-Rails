<% @messages.each do |message| %>
  <h2><%= message.title %></h2><br />
  <%= link_to "Show Message", message_path(message), class: "btn btn-default" %>
<% end %>

<%= link_to "New Message", new_message_path, class: "btn btn-default" %>
